import React from 'react'

const Za = (props) => {
    return (
        <div>
            <h1>Salom, {props.name}</h1>
            <h2>I'm {props.age} years old </h2>
        </div>
    )
}

export default Za
